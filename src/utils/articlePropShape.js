import PropTypes from 'prop-types';

const articlePropShape = {
  title: PropTypes.string,
  date: PropTypes.string,
  image: PropTypes.string,
  preamble: PropTypes.string,
};

export default articlePropShape;
